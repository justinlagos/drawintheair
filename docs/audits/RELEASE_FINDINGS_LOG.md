# Release findings log — New-Term Readiness Release

Things noticed while executing the release gates that were **not** fixed (out of scope).
Append-only. Format: date · gate · finding · suggested owner.

| # | Date | Gate | Finding | Suggested action |
|---|---|---|---|---|
| 1 | 2026-09-07 | 1B | The execution brief `docs/audits/NEW_TERM_RELEASE_EXECUTION_BRIEF.md` referenced by the task does not exist in any checkout on this machine (searched `/`). Gate 1B was executed from the task instructions alone; §0 standing rules could not be read. | Founder: commit the brief to `docs/audits/` so later gates can cite it. |
| 2 | 2026-09-07 | 1B | `CLAUDE.md`, `CONTRIBUTING.md` and `.github/workflows/ci.yml` all said "lint is red on master (9 pre-existing errors)". Lint is actually 0 errors / 162 warnings on cca72f5. Docs fixed in this branch; ci.yml comment fixed in WP1B.2. | None further. |
| 3 | 2026-09-07 | 1B | `.github/workflows/typecheck.yml` triggers only on `main` (push + PR), so it never runs — `main` is not the production branch. It duplicates ci.yml's typecheck+lint. | Removed in WP1B.2 (redundant); noted here for the record. |
| 4 | 2026-09-07 | 1B | `docs/ROLLBACK.md` instructs "Promote" a previous Vercel deployment as the app-rollback path, which contradicts the deploy contract (no manual promotes). Also cites `d355bbf` as the known-good baseline — stale; live is cca72f5. | Rewrite rollback as "revert PR on master → CI → auto-deploy"; reserve Vercel promote for founder-only emergencies with a written incident note. |
| 5 | 2026-09-07 | 1B | `docs/PUBLISH_INSTRUCTIONS.md` line 29 still says Vercel's Production Branch "is one branch (likely `main`)". Stale. | Update to `master` / project `drawintheair`. |
| 6 | 2026-09-07 | 1B | `origin/fix/commercial-day1-leaks` carries 28 pre-squash commits beyond #11/#12/#13 that were not reviewed here; the `bundle/*` remote branches (analytics-classroom-wiring, feat/meta-pixel-capi, feat/persistent-class-learner, feat/ssg-prerender, …) are also unreconciled against master. | Founder to triage; do not merge any of them directly. |
| 7 | 2026-09-07 | 1B | `check:secrets` prints ".env.example: leaked-literal → your-strong-pin-here" (informational, exit 0). Placeholder text trips the literal scanner. | Change the placeholder to a form the scanner ignores, or allow-list `.env.example`. |
| 8 | 2026-09-07 | 1B | 162 ESLint warnings, mostly `react-hooks` compiler rules (`set-state-in-effect`, `purity`, `immutability`) plus unused catch vars and unused `eslint-disable` directives (12 auto-fixable). | Dedicated `chore/lint-warnings` PR; lower the `--max-warnings` ratchet as they go. |
| 9 | 2026-09-07 | 1B | `supabase/repairs/20260716_pilot_class_3760_reconcile.sql` (from PR #11) is a one-off data repair script for a specific pilot class committed to the repo. | Confirm it was run in prod (it is not a migration); consider moving to `docs/` or deleting once confirmed. |
| 10 | 2026-09-07 | 1B | Repo root still carries ~40 historical .md/.docx/.pdf/.jsx clutter files and a `ruvector.db` binary. | Separate cleanup PR; out of release scope. |
| 11 | 2026-09-07 | 2B | `src/lib/observability/sentry.ts`: Sentry session replay is off by default but can be switched on with `VITE_SENTRY_REPLAY=on` (error-only, all text/inputs/media masked). If ever enabled it would also run on child routes; WP2B.1 does not gate it because it is not enabled and is not marketing analytics. | Founder: keep `VITE_SENTRY_REPLAY` unset on the Vercel project. If it is ever wanted, gate it through `isChildRoute()` first. |
| 12 | 2026-09-07 | 2B | `platform/` (Next.js teacher platform) was not reviewed for third-party analytics loaders in this package; it is a teacher surface, but if it ever embeds a child screen the same rule applies. | Check `platform/` for gtag/clarity/fbq/posthog when it next ships. |
| 11 | 2026-09-07 | 2B (WP2B.3) | `public/service-worker.js` network-first strategy races every own-origin `.js`/`.css` against a 6 s timeout and returns `Response.error()` on a cold cache miss. On a slow school link a first visit can fail a legitimately loading chunk. `/mediapipe/` is now exempt; the general case remains. `CACHE_VERSION` (`v10-...-2026-06-20`) not bumped by this package, per DIA-035 deferral. | Review in DIA-035; consider a longer timeout or no race on cold misses. |
| 12 | 2026-09-07 | 2B (WP2B.3) | New `tracker_assets_resolved` analytics event is not on `PH_EVENT_ALLOWLIST` (adding is a privacy review). `wasm_source` / `model_source` do reach PostHog as meta on the already allowlisted `tracker_init_succeeded` / `tracker_init_failed`. | Founder decides whether to allowlist it. |
| 13 | 2026-09-07 | 2B (WP2B.3) | The register asks for a required-hosts list for school IT in the setup guide. After this package tracking needs only the app origin, but analytics, fonts, Supabase and Sentry hosts are still third party and undocumented for school IT. | Track P: publish a required-hosts list. |
| 14 | 2026-09-07 | 2B (WP2B.3) | `docs/HAND_TRACKING_AUDIT.md`, `docs/NIGERIA_RELIABILITY_REPORT.md` and `docs/SECURITY_AUDIT_2026-05-21.md` still describe CDN-only loading. | Historical docs; add a note or leave dated. |
| 11 | 2026-09-07 | 2B (WP2B.4) | The full-screen `CameraRecovery` dialog (permission/no-device/busy/unknown causes) renders a `mailto:help@drawintheair.com` link and a "Back to home" action that does `window.location.href = '/'`. Both can be reached from child routes (`/play`), which brushes against the "no external links in child mode" rule. The new camera-lost panel deliberately has neither. | Product decision: hide the mailto on child routes or gate it behind the parent/adult surface. |
| 12 | 2026-09-07 | 2B (WP2B.4) | `TrackingLayer` positioning toast copy contains an em dash (`Step back a little \u2014 you're doing great!`), against the copy rule; left as is (out of scope). | Fix in a copy pass. |
| 11 | 2026-09-07 | 2B | `parent_controls.allowed_categories` (migration 0004) is defined in the schema and the `ParentControls` type but is never written by the parent dashboard (`src/pages/parent/Children.tsx`) and never read by the play app. There is no agreed mapping from its values to mode ids or the menu's display categories, so a per-child "allowed modes" control does not exist end to end. WP2B.5 enforces the controls that do exist (pause, daily limit) at mode mount. | Product decision: either build the dashboard control and a value-to-mode contract, then add it to `evaluateModeMount`, or drop the column. |
| 12 | 2026-09-07 | 2B | Entitlement (`useParentAccess`) is cached per tab in `sessionStorage` (`dita-parent-has-access`). Anything client side can be edited in devtools; the real revenue backstop would be a server check on the content each paid mode loads. WP2B.5 adds a server recheck on every mode entry but the decision is still made in the browser. | Accept for a children's tracing app, or scope a server-side gate on paid-mode assets later. |
| 13 | 2026-09-07 | 2B | `colour-builder` and `building` are URL-only modes (DIA-024) with no tier decision anywhere. WP2B.5 marks them premium in `modeCatalog.ts` so a deep link fails closed for anonymous visitors. | Confirm this is the wanted tier when DIA-024 is worked. |
| 11 | 2026-09-07 | 2B.6 | 25 of the 28 public functions that read `analytics_events` apply no `traffic_type` filter; only `dashboard_executive_summary`, `dashboard_funnel` and the promote trigger do. Unfiltered: `dashboard_public_proof`, `landing_public_proof`, `dashboard_today`, `dashboard_top_modes`, `dashboard_tracker_health`, `dashboard_errors`, `dashboard_classrooms`, `dashboard_cohort_retention`, `dashboard_cohort_curves`, `dashboard_growth`, `dashboard_engagement_deep`, `dashboard_retention_deep`, `dashboard_latest_sessions`, `dashboard_live`, `dashboard_observability`, `dashboard_ab_results`, `dashboard_export_headline`, `dashboard_friction_engineering`, `dashboard_ingest_latency`, `lios_detect_friction_v1`, `lios_detect_anomalies_v1`, `lios_score_unscored_attempts`. Internal, QA and bot traffic feed those numbers. The WP2B.6 canary is designed around this (NULL device_id, unique event name) but the gap stands. | Fold into the WP2A.2 / DIA-006 dashboard rewrite or a dedicated package: apply the `analytics_events_real`-style predicate in one place. |
| 12 | 2026-09-07 | 2B.6 | The deployed `analytics-digest` edge function has an HTML renderer that was never committed; `platform/supabase/functions/analytics-digest/index.ts` is a stub that emails raw JSON and says so in its header. Redeploying from the repo would regress the email format. | Founder: pull the deployed source into the repo before any redeploy. |
| 13 | 2026-09-07 | 2B.6 | `dashboard_daily_digest.yesterday_count` and `last7_avg_count` were raw, unfiltered event counts (fixed in WP2B.6 because the canary would have dominated them). `dashboard_today()` which the same digest calls is still unfiltered. | Same owner as finding 11. |
| 11 | 2026-09-07 | 2B (WP2B.8) | DIA-032 register entry says the idle-timeout fix (PR #8, 1f142b7) is undeployed. `1f142b7` is an ancestor of the production commit `6a81fdf` and its logic is present verbatim on `release/new-term`; cherry-pick is empty. The 38% orphan rate in the audit was measured with the timeout already live, so the timeout alone did not fix orphaning. | Founder: mark DIA-032 as "deployed, effect insufficient"; re-measure orphan rate after Gate 3 with the SQL in WP2B.8 README, then tune `dita_idle_timeout_ms` or add a visibility-based close. |
| 12 | 2026-09-07 | 2B (WP2B.8) | `word-search` emitted 435,350 `stage_started` events in 120 days (next largest event in any mode is 7,287). Almost certainly a per-frame or per-render emit in `wordSearchLogic.ts`. Inflates the event table and any "stages started" metric. | Fix in a dedicated PR (emit once per chapter change); consider deleting the storm rows before any engagement claim. |
| 13 | 2026-09-07 | 2B (WP2B.8) | balloon-math, rainbow-bridge, gesture-spelling, word-search and colour-builder have had no `mode_started` since 25 June 2026 in production; only calibration, free, pre-writing, tutorial and sort-and-place (to 25 Aug) still run. Their attempt writers work but produce nothing because the modes are not reached. | Confirm the intended menu set for the term; if these modes are meant to be live, that is a menu or entitlement bug, not a measurement one. |
| 14 | 2026-09-07 | 2B (WP2B.8) | `colourbuilder_match_made` is declared in the EventName union and the productive-action list but no code emits it; colour-builder has one `stage_started` ever. | Remove the dead event name or wire it; low priority. |
| 15 | 2026-09-07 | 2B (WP2B.8) | The child-bound "win event" mirror in analytics.ts writes a second learning_attempts row for `balloonmath_balloon_popped`, `rainbowbridge_match_made`, `wordsearch_word_found` and `spellingstars_word_complete` when a parent child is selected, on top of the `item_dropped` row for the same action. Parent dashboard completion counts for those modes are therefore inflated. Left untouched here because the package must not change dashboards. | Decide whether the parent rollup should count actions or items; then drop those four from WIN_EVENTS in a dashboard-owned PR. |
| 11 | 2026-09-07 | 2B | Production `public.form_submissions` had table privileges granted in full to `anon` and `authenticated` plus a public INSERT policy (WITH CHECK true), so the anon key could insert rows directly, bypassing any validation or rate limit. Tightened by the WP2B.2 migration (not yet applied). The same broad grant pattern may exist on other tables created outside the repo's migrations. | Founder: after WP2B.2 lands, run a one-off grant audit (`information_schema.role_table_grants where grantee='anon'`) and log anything unexpected. |
| 12 | 2026-09-07 | 2B | `public.form_submissions` is not created by any migration in `supabase/migrations/` (created by hand or by the retired platform repo). The WP2B.2 migration includes a `create table if not exists` with the production shape so staging can rehearse. | Consider adding it to `supabase/baseline` when that is next regenerated. |
| 13 | 2026-09-07 | 2B | In-game micro-surveys (`src/features/feedback/HappinessCheck`, `ExpectationCheck`, `ExitFeedbackModal`, `StuckHelp`) post to the lead pipeline as `type: 'feedback'` from child routes. They now hit the edge function rate limit (5 per IP per hour) which a classroom sharing one IP could exceed; the rating is also logged to `analytics_events` so nothing is lost, but the form_submissions copy will be partial. | Decide whether child-route feedback belongs in `form_submissions` at all; if yes, exempt `feedback` from the IP limit or raise it. |
| 14 | 2026-09-07 | 2B | `src/pages/classmode/TeacherDashboard.tsx` is unrouted legacy (replaced by TeacherClassConsole) but still compiled and shipped. Its link was repointed in WP2B.7 rather than deleting the file. | Delete in a cleanup PR. |
| 15 | 2026-09-07 | 2B | Historical docs still describe `app.drawintheair.com` as the live platform: `docs/GO_LIVE_STABILIZATION_REPORT.md`, `docs/SECURITY_HARDENING_REPORT.md`, `docs/SECURITY_AUDIT_2026-05-21.md`, `docs/ROUTE_MAP.html`, root `e2e-test-report.html`. Left as records. | Add a "superseded" banner or move to `docs/archive/` in the cleanup PR. |
| 16 | 2026-09-07 | 2B | `scripts/check-csp.mjs` still requires `https://script.google.com` in connect-src for the Google Sheets form fallback (`VITE_SHEETS_ENDPOINT`). Unknown whether that endpoint is configured in Vercel; if not, both the CSP entry and the fallback code are dead weight. | Founder: check Vercel env for VITE_SHEETS_ENDPOINT / VITE_LEADS_ENDPOINT; if unset, remove tiers 2 and 3 and the CSP entry in a follow-up. |
| 11 | 2026-09-07 | 2B (WP2B.9) | robots.txt disallows `/parent/*` and `/admin` but not `/teacher/*` (teacher signup, login and dashboard are client-gated SPA routes with the same posture as `/parent/*`). Not changed in WP2B.9 to keep the fix to the audited lines. | Product decision: add `Disallow: /teacher$` and `/teacher/` in a follow-up; `/teachers` marketing page must stay allowed. |
| 12 | 2026-09-07 | 2B (Track P) | `src/pages/seo/FreeResourcesPage.tsx` A to Z grid links `/classroom-guides/letter-<a..z>.pdf` (26 files) and none exist in `public/classroom-guides/`; every letter card is a dead download on production. `PAGE_META.freeResources` also promises number worksheets that do not exist. | Generate the worksheets with `scripts/generate-brand-pdfs.py` or hide the grid until they exist. |
| 13 | 2026-09-07 | 2B (Track P) | `src/pages/teacher/TeacherViews.tsx` `SETUP_STEPS` tells teachers children "tap their picture ... with no names to type". On master `StudentJoin.tsx` asks for a first name; the picture join is on `feat/persistent-class-learner`. Same defect as register item C57 but in the live dashboard. Not changed: it becomes true if that package ships in Gate 3. | If the persistent-learner package does not ship, rewrite the four steps to describe the typed-name join. |
| 14 | 2026-09-07 | 2B (Track P) | `scripts/generate-brand-pdfs.py` still carries the old copy for the two deleted guides (01 quick start, 05 parent pack) and the age range "3-8". | Fix the script copy before regenerating any PDF. |
