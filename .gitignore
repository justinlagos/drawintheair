# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables — NEVER commit .env files
# .env.example is safe to commit (it contains no real secrets)
.env
.env.*
!.env.example

# Internal planning and design documents (not for production or public repo)
internal/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code internal state (worktrees, settings, scratch)
.claude/

